# BetterYeah AI 架构分析

> 证据来源：`ai-docs.betteryeah.com` 全站手册 + 43 篇更新日志 + `www.betteryeah.com` 三个产品页。
> 2026-09-13 补充官方正文复核和已登录 Chrome 页面观察，见[浏览器架构证据](research/betteryeah-browser-architecture-2026-09-13.md)。证据分为 **O：官方明示**、**B：浏览器可见**、**D：本项目设计**、**U：未知**；界面入口不能证明执行效果或私有后端实现。
> 读取规则：本文记录竞品观察，不是本项目的最终决策；安全、租户、凭据和恢复语义以 ADR-001 至 ADR-004 为准。

## 1. 产品拓扑

八个一等公民：

```
                    Agent（对话式智能体）
              角色设定 / 技能 / 任务 / 知识 / 数据
                 |           |            |
      工作流 Flow-as-Service  插件 Plugin  知识库 KB / 数据库 DB
                 |           |            |
                 +----- Skill 服务包（工作流/插件；发布后供 Agent 调用）-----+
                 |
   MCP 服务出口 | 发布渠道（API/SDK/钉钉/飞书/企微/微信） | 效果观测
```

分析判断：Agent 是对话式编排层；Flow 是显式图编排执行层（可包含非确定性的 LLM 节点）；插件是原子工具层，知识库/数据库是状态层，MCP/渠道是出口层。这是本研究的分层语言，不是对竞品内部运行时的实现断言。

**O：** [2026-03-05 官方更新](https://ai-docs.betteryeah.com/更新日志/2026-03-05%20更新日志.html)明确：工作流、插件可以组成 Skill 包，配置包描述和工具用法后发布，并在 Agent 中调用。**B：** 2026-09-13 已登录页面可见 Agent 的 Skill 列表、已有绑定、启用开关、添加服务和新建入口；Skill 编辑器独立展示包描述、包说明、工具成员、引用资源及输入描述，添加工具菜单提供插件/工作流。详见[技能系统深研 §1.1](10-技能系统深研.md#11-skill-包编辑器的可见结构b)。**U：** 保存载荷、参数映射、工具选择、依赖更新传播、撤销和在途运行语义仍待实验，不能由“可绑定”直接推导。

## 2. Agent 运行时模型

| 构件 | 机制 | 关键细节 |
|---|---|---|
| 角色设定 | 结构化 Prompt | 六段骨架 `# 角色 / # 目标 / # 技能 / # 要求 / # 工作流 / # 输出格式`；结构化模式七主题，每主题可调权重 |
| 技能 | 插件 \| 工作流 \| 子Agent \| 已发布 Skill 包 | 插件/工作流说明书驱动选择；Skill 发布与 Agent 绑定有 O/B 证据，精确路由/参数/版本传播仍为 U |
| 任务 | 异步调用技能 | 不阻塞对话，完成后推送；支持定时；预设任务对使用者默认关闭且不可修改 |
| 知识 | 强制查询 \| 按需查询 | 按需模式需「知识库说明书」 |
| 数据库 | 关系型 | 存储和检索结构化数据；Agent 可绑定数据表，Flow 数据库节点支持增删改查 |
| 变量 | `{{character}}` | 会话前由使用者填，或 API `inputs` 注入；每次新会话赋值一次 |

公开材料能确认：角色设定、技能说明与输入描述会参与 Agent 的对话与技能选择，且说明书质量会影响调用效果。planner/executor 是否分离、是否存在显式状态机、内部路由/反思实现均未公开；在取得官方契约或受控实验前，不能把它们写成产品事实。

## 3. 工作流引擎

节点清单（手册穷举）：

```
开始(表单 | Webhook) → 输出
LLM · 知识库(QUERY_DATA / INSERT_DATA) · JavaScript&Python(CODE) · API
Flow(子流程) · 逻辑分支 · Plugin · 意图识别 · 文本 · 图文分类器 · Loop循环 · 条件循环
```

- **分支**：JS 条件表达式（`api_1.success === true`），自上而下短路，首个命中即执行且后续不再判断，兜底走最末 Else。跨分支节点不可互访
- **变量引用**：`start.content` 点路径。分支条件里变量不加双花括号——与 Prompt 内插值语法不一致，是易错点
- **LLM 节点**：模型全家桶（ChatGLM3 / Moonshot 8k-128k / 千帆 ERNIE-Bot-4 / GLM-4 / 讯飞 / 混元 / qwen-plus / DeepSeek-R1、V3）；`创造性` = temperature；`上下文` 接受 role/content 数组 JSON；JSON 输出仅 OpenAI 系可用；多模态经上下文传 `image_url` / `video_url`
- **运行方式**：单次 / 批量（Excel 多组入参）/ 定时。定时任务「运行参数是创建时设定的，无法随着时间的推移而动态变化」
- **多环境**：开发→测试→生产，环境变量隔离知识库/子流程/模型，逐级发布不覆盖其他环境。「一旦激活，无法还原为普通Flow」
- **依赖与复制（O）**：逐级发布时可选择子工作流的对应环境；同空间复制 Agent/Flow 可保留原资源关联。[2026-01-29 官方更新](https://ai-docs.betteryeah.com/更新日志/2026-01-29%20更新日志.html)未公开依赖版本解析和发布事务的内部实现。
- **数据库节点（O）**：支持 SQL 增删改查；文档要求 LIMIT，最多处理 500 行，缺失时拦截。[调用数据库](https://ai-docs.betteryeah.com/数据库/调用数据库.html)。这不证明 Agent 自动写入的确认策略，也不要求本项目采用任意 SQL。
- **调试**：节点级日志（入参/出参/耗时）；除开始、输出、分支外支持单节点执行
- **集成**：Flow-as-a-Service，每个 Flow 自带 API；Workspace 级 API Key，重新生成后旧 Key 立即失效

## 4. 知识库 / RAG 链路

```
上传(Word/Excel/PPT/PDF/网页/图片/视频)  多模态大模型 + OCR + 声轨识别
  ↓
自动分段  Word类(标题+段落, >500字切) · PPT类(一页一段) · Excel(行+表头)
          通用类(\n\n, >500字切) · QA分段 · JSON分段
  自定义：分段标识符(支持转义字符) + 每段最长字数(建议500)
  ↓
Embedding  双模型(多语种 / 纯中文) + text-embedding-3-large + jina-embedding-V3 + jina-clip-v2
  单段失败仅异常标记，可手动重分段或「AI一键处理」
  ↓
三路查询(可多选组合)
  语义查询  向量相似度，跨语言，弱于精确词与专业术语
  关键词查询 段落维护的关键词精确匹配，无维护则查不到
  全文查询  分词 + 词频权重排序
  + 近义词表
  ↓
结果重排(开箱即用，免部署免第三方 KEY，宣称准确率 +69%)
  ↓
知识匹配最大结果数 → 注入 Agent；无命中则回退训练数据或转调技能
```

检索侧仍有待补证实的边界：三路查询的融合权重或 RRF 策略、Agent 编辑页的相似度阈值、parent-child 策略及元数据筛选执行语义尚未确认。Flow 知识库节点已公开属性筛选。

**O：** 500 字是 Word/通用自动分段模式的切分规则；自定义分段允许用户设置每段最长字数，500 字仅为建议，不能称为全平台硬上限。[文档知识处理](https://ai-docs.betteryeah.com/知识库/文档知识处理.html)。文档/问答知识另有手动版本记录与历史恢复，恢复时会自动保存当前内容为新版本；是否重建索引、何时切换在线检索仍为 **U**。[2025-12-18 官方更新](https://ai-docs.betteryeah.com/更新日志/2025-12-18%20更新日志.html)

## 5. 工具生态

| 层 | 机制 |
|---|---|
| 官方插件 | 40+，含网页解析·Bing/Google搜索·AI识图/生图·小红书/抖音/淘宝抓取·通用文档解析·大文档问答·图表绘制·AI图生视频·HTML转图片·飞书云文档系列 |
| 自定义插件 | 上传 API 文档自动解析生成，或手动搭；另支持 MCP（自定义添加 / 同步第三方 MCP） |
| 插件鉴权 | ① 静态 Service Token / Api Key ② **工作流鉴权**——用一个 Flow 做自定义鉴权，配置入参 + 失效时间，到期平台自动重新鉴权 |
| Skill 服务/包入口 | O/B 已确认工作流/插件成包、发布和 Agent 绑定；版本固定、更新/撤销传播与运行生命周期仍待验证 |
| MCP 出口 | 工作流 / 三类知识库 / 插件均可暴露为 MCP 服务；ID 命名影响 AI 调用成功率 |
| A2A | 自建 Agent 可发布为 A2A 服务，也可连接外部 A2A Agent 后用于多 Agent 编排 |

「工作流鉴权」是聪明设计——用平台自身编排能力解决 OAuth/签名/换 token 这类长尾鉴权，避免为每种鉴权写死适配器。值得直接借鉴。

MCP 方向早期单向（只做 Server），2025-07-31 补上「同步第三方 MCP」后才具备 Client 能力。

**O：** [创建 MCP 服务](https://ai-docs.betteryeah.com/MCP服务/创建MCP服务.html)明确工具可来自工作流、插件和三类知识库，工具描述、code 与输入描述可独立编辑；这与 Agent 调用外部 MCP 的接入方向不同。[2025-07-03 官方更新](https://ai-docs.betteryeah.com/更新日志/2025-07-03%20更新日志.html)确认 A2A 的发布和连接双向能力，后于 [2025-10-23](https://ai-docs.betteryeah.com/更新日志/2025-10-23%20更新日志.html)并入 Agent 目录。

**本项目差距（D/交付记录）：** [完整应用交付总图](plans/2026-09-02-complete-agent-application-delivery.md)记载产品迁移 039 的 Skill Pack 目前提供版本化说明并注入模型上下文。这尚不能覆盖竞品已公开的“包内工作流/插件供 Agent 调用”。需要补齐成员选择、精确成员快照、可调用操作及执行收据；底层 closure 的成员契约也不能替代产品 UI/API/Run 的闭环验收。`InstructionSkillRelease` 的过程上下文与可调用工具包须保持不同语义。

## 6. 发布与渠道

```http
POST https://ai-api.betteryeah.com/v1/oapi/agent/chat
Content-Type: application/json
Access-Key: <workspace access key>
Workspace-Id: <workspace id>

{ "robot_id","conversation_id","content","inputs":{},"response_mode":"blocking|streaming" }
```

响应是统一 task 事件流，设计得好：`tool_type` + `status` 双字段即过程透明化的最小充分设计。详见 `06-API契约.md`。

渠道身份映射（`user_type` / `user_id`）：

| 渠道 | user_type | user_id |
|---|---|---|
| 企微机器人 | `WEIXIN_WORK_APP_BOT` | 成员账号 |
| 钉钉机器人 / 流式 | `DINGTALK_ORGAPP_ROBOT` / `DINGTALK_STREAM_ROBOT` | `{senderCorpId}:{senderStaffId}` |
| 飞书机器人 | `FEISHU_APP_BOT` | `{union_id}` |
| 微信客服 / 公众号 / 企微应用 / 钉钉网页 | `WEIXIN_KF` / `WEIXIN_MP` / `WEIXIN_WORK_APP` / `DINGTALK_ORGAPP_WEB` | 暂未支持 |

## 7. 权限模型与文档中的 fail-open 语义

机制：Workspace 隔离 + 权限变量（`user_type` + `user_id`）+ 企业侧权限回调地址（SPI `action=external_user_auth`），可控资源五类：`agents` / `flows` / `plugins` / `a2a_agents` / `datasets`。

手册两处出现同一句：**「如果没有设置或设置不全，则权限管控不会生效」**，且 `allow` 不填「代表不做任何权限限制」。

**O：** [Agent 权限配置](https://ai-docs.betteryeah.com/发布Agent/Agent权限配置.html)描述 `allow` 缺失时不限制权限。**D：** 这对依赖回调裁剪资源的集成构成 fail-open 风险；不能仅凭文档推断所有渠道已经暴露全部知识，实际可访问范围还需结合其他权限层验证。**U：** 回调超时、缺失身份及各渠道的完整拒绝/放行行为仍须受控实验。

项目决策：外部兼容面不等于默认风险语义。G0/G1 只采用 fail-closed，不激活 compatibility profile；未来只有独立的版本化 safety envelope、migration 与验收门完成后，才可研究经审批的受限兼容行为，并且仍须完成无 `user_id` 渠道/非 public 资源禁配、发布可见性扫描、审计与缓存失效。详见 [ADR-001](./adr/001-兼容性与安全默认值.md)。

## 8. 可观测与评测

- **Flow 日志（O）**：每次运行及节点明细包含入参、出参、耗时，支持单节点调试；[官方日志页](https://ai-docs.betteryeah.com/Flow/Flow的调试与日志.html)没有给出完整字段/保留契约，不能由其未列出 token/成本推断产品没有这些指标。
- **Agent 日志**：平均首字响应时长 / 平均耗时时长 / 每轮首字响应时长 / 内容输出时长
- **批量测评（O）**：[2026-03-05 官方更新](https://ai-docs.betteryeah.com/更新日志/2026-03-05%20更新日志.html)描述上传测试集、自定义评估流程和自动测评，评估输出要求 `{ "score": 95, "analysis": "", "overall_evaluation": "" }`。
- **效果观测导航（B）**：2026-09-13 已登录页面可见指标监控、在线质检、批量测评、评估器，见[浏览器证据](research/betteryeah-browser-architecture-2026-09-13.md)。在线质检创建显示企业版限制，本账号未能执行实验；采样、执行、数据关联及故障策略仍为 U。
- **效果监控**（2025-01-09）：自定义监控规则 + 自动生成图表 + 模板 + 监控明细与日志；2025-04-17 加高级编排模式（用工作流设计监控指标）
- **数据统计**：整体用量/异常、Agent 用量/异常、工作流用量/异常、知识库积分、应用/知识库明细（分组 + 导出）
- **监控告警**（2024-08-15）：积分余量 / 积分消耗过快 / 应用运行错误 / 应用响应时长超时 / 空间到期；通道为短信、钉钉群机器人（支持 @ 群成员）、电话
- **模型对比**：同一问题跨模型对比；**批量测试集**：跑测试集比对预期答案并给调优建议

公开资料确认可使用编排流程自定义评估；在线质检入口也已可见。不能再称“只有离线评测”。生产流量采样、持续回归、A/B 与 trace 归因是否支持以及具体契约仍为 **U**。

## 9. 生态全过程映射

| 阶段 | 能力 | 成熟度 |
|---|---|---|
| 需求→原型 | 一句话生成 Agent、AI 写提示词、200 案例训练库 | ★★★★ 冷启动强 |
| 构建 | 零代码编排、16 类节点、Code IDE、7 套模板 | ★★★★ |
| 接知识 | 多模态解析、无损语义分段、5 个 Embedding | ★★★★★ 最强项 |
| 接工具 | 40+ 插件、自定义插件、工作流鉴权、已发布 Skill 包绑定 | ★★★★；运行细节待验证 |
| 接数据 | 平台关系型数据库、Agent 表绑定、Flow 增删改查 | ★★★；事务和授权边界待验证 |
| 调试 | 节点级日志、单节点执行、命中测试、Mock 变量、权限变量模拟、批量调试 | ★★★★ |
| 评测 | 批量测评 + 自定义评估器 + 模型对比 + 在线质检入口 | ★★★；在线执行语义待验证 |
| 发布 | 8 渠道 + SDK + MCP 出口 + A2A + 三环境 | ★★★★ |
| 运营 | 定时任务、异步任务推送、日志、告警 | ★★★；定时参数静态，成本字段待验证 |
| 治理 | Workspace、权限回调、角色配置、私有化、知识文件版本恢复 | ★★；Agent 版本/回滚契约待验证 |
| 复用 | Skill 包及 Agent 绑定、MCP 服务、模板、应用复制沿用资源 | ★★★；依赖版本传播待验证 |

## 10. 缺口清单

此处区分已公开限制与待验证边界，不把未公开信息当作产品不存在的证明。本项目安全、版本、可观测和恢复设计的具体取舍以 ADR 为准。

| # | 缺口 | 证据 |
|---|---|---|
| G1 | U：Agent 发布版本、历史与回滚契约尚未确认 | 未发现公开协议不能推出没有功能；知识文件恢复已由 2025-12-18 更新确认 |
| G2 | 分支隔离，跨分支不可读 | 逻辑分支节点页明示 |
| G3 | U：Run/节点级 token、成本字段与保留范围待验证 | 官方日志页列出耗时等字段，并未声明完整字段集 |
| G4 | O：数据库被官方用于持久记忆；U：专门记忆生命周期与隔离策略待验证 | [欢迎页](https://ai-docs.betteryeah.com/)和[数据库介绍](https://ai-docs.betteryeah.com/数据库/什么是数据库.html)；不能称长期记忆仅有 session 手写 |
| G5 | Agent 检索的阈值、融合参数与编辑页元数据筛选语义未公开；Flow 知识库节点已提供属性筛选 | 知识查询方式页与 Flow 节点资料 |
| G6 | O：Word/通用自动分段按 500 字切分，Excel 单表头限制；自定义分段长度可配 | [文档知识处理](https://ai-docs.betteryeah.com/知识库/文档知识处理.html)；500 不是所有模式硬顶 |
| G7 | 权限 fail-open | 「设置不全则权限管控不会生效」 |
| G8 | 定时任务参数静态 | 运行与集成页明示 |
| G9 | B：在线质检入口存在；U：在线采样、持续回归、A/B 与 trace 归因 | [2026-09-13 浏览器证据](research/betteryeah-browser-architecture-2026-09-13.md)；入口不等于运行实测 |
| G10 | 未见公开 planner / reflect 配置或运行时证据；不能据此断言内部不存在 | 全站公开资料未给出该契约 |
| G11 | Python 侧 `betterAI.session` 不可用 | Code 节点页明示跨语言不对等 |
