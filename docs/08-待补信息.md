# 待补信息

手册中只以截图形式存在、正文无文本的内容。不阻塞 P0-P2，P4 前需补齐。

| # | 缺失项 | 影响阶段 | 获取方式 |
|---|---|---|---|
| 1 | MCP 服务连接地址域名/路径 + 完整请求头 JSON | P4-4 | 产品内「使用MCP服务」页复制 |
| 2 | 定价页四类权益具体行项与数值（用量权限 / 精品功能 / 外部应用能力 / 服务支持） | P5-5 | `betteryeah.com/pricing` 客户端渲染后截图 |
| 3 | 数据库表头可选字段类型清单 | P2 / 004 迁移 | 产品内「创建数据表」界面 |
| 4 | 问答知识 xlsx 官方模板 | P2-10 | 问答知识库下载模板 |
| 5 | 效果监控模板列表 | P5-2 | 效果监控「创建时可选模板」 |
| 6 | QUERY_DATA / INSERT_DATA 节点返回结构 | P1 / P2-12 | 工作流运行日志出参 |
| 7 | 官方插件完整清单（手册标注「....更多去平台体验」） | P4-1 增量 | 插件市场列表 |

## 已知但需自行定规格的项

手册未公开、复刻时必须自己定死：

| 项 | 建议默认 |
|---|---|
| 三路检索融合权重 | RRF，k=60，语义:关键词:全文 = 1:1:1，可配 |
| 相似度阈值 | betteryeah SDK 已暴露 `similarity` 默认 0.4，沿用 |
| 最大结果数默认值 | SDK `maxResultNum` 默认 3，沿用 |
| Code 沙箱资源限额 | CPU 1 核 / 内存 512MB / 时长 60s / 默认禁网 |
| 会话上下文窗口 | 按模型上限的 70%，超出触发摘要压缩 |
| 日志保留 | 运行日志 90 天，ClickHouse 统计 永久 |
