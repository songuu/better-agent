# G1-A1 Capability Closure / Policy / ResolvedPlan kernel

> Status: T1–T2, the T3.1 manifest graph and the source-preimage portion of T3.2 implemented locally; final T3.2 closure compilation, T4–T6, PostgreSQL validation and deployment remain pending.
> Updated: 2026-09-02
> Planning depth: P3 for the local kernel; P4 for eventual deployment. Implementation risk: L4.

## 1. Product outcome and slice boundary

Better Agent is the complete Agent application defined in `docs/07-实施计划.md`, modeled on `ai.betteryeah.com`. The reference project `E:/project/ai/agent` supplies deployment mechanics only. The final application must provide real editing, publishing, execution, persistence and observation through `songuu.top`; a documentation site, healthy database, green CI or placeholder interface does not satisfy that goal.

This plan implements the first dependency of that application, not a reduced final deliverable. G1-A1 supplies a single deterministic capability kernel shared by Flow and Agent. G1-A2 through G1-A8 then add fixed resources, durable execution, authenticated API/browser projections, production evaluation and a real vertical acceptance path. The broader product inventory remains in the existing implementation plan.

## 2. Entry evidence and current uncertainty

- `docs/plans/.handoff/active-sprint.json` records a passed, host-attested G0-08 receipt at review generation 3. The historical receipt hash is `sha256:b2f5b229b01162058cd364e06bfcdd2a9b98556392252f304cd9a235f1840668`.
- That record supersedes the older prose saying no receipt exists, but it is not a new attestation of subsequent source changes. Do not edit the historical receipt or its evidence to update status text.
- Commit `a1ce87d133dfaa1edf482ab9935604b7e5a2bd5d` has a successful [CI run 33581446892](https://github.com/songuu/better-agent/actions/runs/33581446892), including the executable architecture gate and six PostgreSQL suites.
- On 2026-09-02 the historical verifier could not connect to its configured local authority database at `127.0.0.1:55433/tech_persistence`. A temporary connection to the remote database at the same port produced a readback mismatch; it is not proof that the original local authority record is invalid. The tunnel was closed without authority writes.
- Local Docker startup encountered stale Windows sockets. Recovery preserved those sockets under backup directory names. A later Docker log recorded an external UI factory-reset action; its effect on the authority database is unknown. Docker recovery is paused pending coordination with the user. The existing VHDX file is present; file presence alone does not prove database integrity.
- Historical G0 admission remains granted for its recorded subject. The unavailable original database does not automatically revoke that result; the remote mismatch does not establish invalidity of the local record. Pure deterministic TDD may proceed under the user's existing implementation authorization. Current-source PostgreSQL integration, complete gates and applicable independent acceptance are still required before new completion, merge or release claims. Never manufacture authority rows from the local receipt copy or use the old receipt to attest new source.

## 3. Technical approach

Reuse `packages/domain-contracts` for closed public schemas and `packages/release-core` for deterministic publishing primitives. Reuse the existing RFC 8785 canonicalizer and release registry boundaries. Introduce no network activity, secret resolution or runtime execution into compilation.

### Options considered

| Option | Decision | Reason |
|---|---|---|
| Extend the existing release kernel and schemas | Selected | Reuses canonicalization, typed pins and publishing contracts without duplicating security semantics. |
| Give Flow and Agent independent compilers and policy implementations | Rejected | Two authorization interpretations can diverge on nested capabilities. |
| Start with a working-looking Studio backed by placeholder closures | Rejected | Does not meet the existing safety or end-to-end acceptance contract. |

### Contract interfaces

| Contract | Current implementation | Required result | Consumers |
|---|---|---|---|
| Binding path / resource node identity | Structural string and segment schemas | Canonical byte profile, recomputation, exact equality and collision rejection | Publisher, closure loader, ResolvedPlan, Flow/Agent call membership |
| Effective capability policy | Closed egress/budget/ceiling/demand schemas and pure meet/resolve implemented in T2 | Wire verified registry and admission facts into the shared kernel in T3–T5 | Closure compiler and admission |
| Compiled closure | Structural schema / references | Deterministic graph validation, canonical hash and exact pinned dependencies | Release registry, Flow compiler, Agent compiler |
| ResolvedPlan | Target contract, not an implemented admission kernel | Immutable narrowing of a verified closure under typed deployment and current authorization | Worker, API, future Studio execution |

### Canonical identity profile to freeze before implementation

- Define exact numeric segment tags, field tags and field order for `binding-path-lp-utf8/1` in the architecture contract. Lengths and segment counts use fixed-width unsigned big-endian integers; tags and fields form a closed grammar, not an extensible free-form map.
- Preserve business segment order. Require exactly one initial root; owner kinds and pin kinds must agree with their closed segment variants. Root contract hash remains the semantic seed hash, not the final compiled hash.
- Hash complete pins, including workspace, resource kind, resource ID, version ID, contract hash and `binding_mode=pinned`. Never deduplicate on a bare version ID or local binding ID.
- Reject malformed Unicode, accessors, unknown fields, mutable aliases, malformed/noncanonical digest text and configured absolute size/depth/count limits before hashing. Do not introduce delimiter concatenation or a second digest encoding.
- Freeze independent byte/digest vectors for every segment variant and adversarial separators/Unicode. Identity validation must distinguish an incorrect digest, duplicate identity and an actual conflicting canonical payload.

## 4. Ordered implementation tasks

No task is marked `[P]`: the kernel is L4, tasks share contracts and each consumes preceding results. Implementation is serial; independent review remains separate.

- [ ] **T0 — Reconcile provenance and restore validation capacity.** Verify original authority readback when its environment is available, without modifying the record; provide a working disposable PG16 test environment for current-source validation. This is not a new prerequisite for pure T1–T5 TDD and does not revoke historical G0 admission. Files: control-plane evidence under `docs/plans/.handoff/`; distinguish stale status prose from signed historical evidence. No database reinitialization, receipt replacement or broker changes are implied.
- [x] **T1 — Freeze and implement canonical identities.** Write failing fixed vectors and malformed-input tests first, then implement binding-path/resource-node computation and verification. Files: `docs/architecture/compiled-capability-closure-v1.md`, `packages/release-core/src/closure-identity.ts`, `packages/release-core/src/errors.ts`, `packages/release-core/src/index.ts`, `packages/release-core/test/closure-identity.test.ts`; narrowly adjust domain schemas only where required. Depends on recorded G0 admission, not local authority availability. Risk: L4. Local evidence: 56 new identity regressions plus 33 existing release-core tests pass; fresh zero-cache `pnpm check`, 31 architecture tests and five-view review pass. See `.handoff/g1-a1-identity-debug-journal.md`; no full G1/production acceptance claimed.
- [x] **T2 — Implement versioned policy meet.** Closed egress/budget/ceiling/demand vocabularies; exact credential provider/audience, full required scopes, common principal modes, exact operation allow-sets, clearance min/taint max, side-effect/approval and all budget dimensions. Joint token minima and complete output budgets also enforced. Files: closure contract, domain policy schemas/tests, release-core policy/snapshot modules/tests. Depends on T1. Risk: L4. Evidence: 68 new release tests + 69 new domain tests; package totals 157/122; final fresh zero-cache `pnpm check` and 31 architecture tests pass; five-view review P0/P1/P2=0. See `.handoff/g1-a1-policy-review.md`. This is pure local kernel evidence, not new PostgreSQL/CI/host-attested or production acceptance.
- [ ] **T3 — Compile and verify the pinned graph.** Enforce kind-specific configuration, recursive dependency pins, cycles, absolute resource limits, root identity, nested closure hashes, forced/optional availability, stable semantic-set ordering and collision detection. Files: closure compiler/loader modules and tests in release-core; closure domain schemas where needed. Depends on T2. Risk: L4.
  - [x] T3.1: prepare/verify the bounded pinned dependency graph from an exact manifest snapshot. Implemented with 81 new graph regressions (release-core total 238), including full-pin/manifest equality, cycles, longest-path depth, 256-node/1024-edge boundaries, hostile input and complete artifact verification. This intermediate artifact does not establish root/nested body provenance or enable publication. See `.handoff/g1-a1-pinned-graph-review.md` for final validation and review evidence.
  - [ ] T3.2: derive graph inputs from kind-specific immutable source preimages, compile canonical Binding paths and path policies, validate nested closure/compiled hashes and required/optional paths; integrate the final closure with T4. T3 stays incomplete until these obligations are implemented and tested. **Partial local evidence:** Agent/Flow source preparation and complete-artifact verification, semantic seed/direct manifest derivation and closure-hash-bound compiled-hash primitives now have 81 source regressions; release-core totals 319/319, domain-contracts 127/127 including five recursive Flow parsing regressions. Source parsing is lossless before explicit metadata exclusions, and recursive schema dispatch no longer reparses condition-loop bodies exponentially. Fresh zero-cache `pnpm check`, 31 architecture tests and five-view review pass. See `.handoff/g1-a1-executable-source-review.md`. Leaf/operation source adapters, verified nested closure bodies, final Binding expansion/path policy compilation, registry provenance and publisher integration remain open; a supplied closure hash is not proof of a verified closure.
    - Operation declaration increment: real input/output JSON documents now produce versioned operation preimages and complete pins; concrete knowledge/database/plugin/Flow/SubAgent Bindings check declaration compatibility, while Skill Pack still requires its separate member route. **60 new regressions**, release-core **379/379**, domain **127/127**, fresh zero-cache `pnpm check` and architecture tests **31/31** pass. This does not validate JSON Schema execution, resource implementation/provenance, GateSpec coverage or nested closure bodies. See `.handoff/g1-a1-operation-source-review.md`; final T3.2 remains open.
    - Typed leaf-source increment: Knowledge/Database/Plugin/A2A bodies now bind complete source preimages, pins, component hashes, operations, intrinsic demands and exact Binding compatibility. Database user predicate/order/additional-filter columns cannot bypass the allowlist or read/output classification. **74 new release regressions + 8 domain cases**, package totals **453/453** and **135/135** pass. Full-repository validation and independent final review are recorded in `.handoff/g1-a1-leaf-source-review.md`. This does not establish registry/implementation provenance, effective path policy, JSON Schema validation, runtime execution or final closure completion. Remaining typed adapters include Instruction Skill/Strategy/Skill Pack; T3.2 stays open.
    - All-kind Schema wiring increment: typed structure now validates Agent root/GateSpec/Binding contracts, every recursively nested Flow node/human gate, all four leaf primary and Plugin/A2A nested operations, and Skill Pack envelope/member/exposure contracts. Up to 8,194 schemas compile in one 8 MiB, 5-second fail-closed worker batch; canonical per-field evidence is chunked 1,024 entries without collapsing duplicate declarations. Release-core **729/729**, repository `pnpm check` and architecture mutation tests **31/31** pass after manifest synchronization. This still does not compile final Binding paths/policies/routes, validate live execution instances, prove registry provenance or deliver the application; T3.2 remains open.
    - Root Binding path increment: an internal compiler step now derives every Agent root Capability Binding path from a prepared executable artifact, registers canonical identities, keeps disabled Bindings in the immutable namespace and emits a separately named source-level disabled set. It deliberately exposes no standalone hash or public pseudo-contract. **12 focused regressions** and release-core **741/741** pass; security/architecture/quality/test reviews converge at P0/P1/P2=0. Nested Flow/Pack/SubAgent paths, effective policy and final closure remain open.
    - Flow node path increment: `flow_node` identities now bind the globally unique Flow `graph_id` as well as `node_id`; the Flow source contract fixes the entry graph to `root` and rejects duplicate graph IDs anywhere across case/else/loop recursion. The private compiler derives sorted, deeply frozen paths for root, branch, else and loop-body nodes from raw validated source, preserves the complete ancestor chain and admits the exact 4,096-node boundary under the composed 8,192-entry registry budget. Binding path `/1` was explicitly reset before any publisher/persistence exists; after persistence, further byte-profile changes require a new version. Release-core **764/764**, domain **155/155**, fresh zero-cache `pnpm check` and architecture tests **31/31** pass; five-view review converges at P0/P1/P2=0. Nested dependency prefixes, Skill Pack/SubAgent segments, effective policy and final closure remain open.
    - Direct Agent→Flow prefix increment: a private two-source adapter revalidates the Agent and Flow independently, requires exact target full-pin equality, registers the complete root Binding namespace, and compiles `root → binding → flow_node...` through one closure-local identity registry. Multiple root Bindings to the same Flow remain isolated; source-disabled Bindings remain addressable; the composed exact 4,096-node boundary passes. Focused tests are **37/37** and release-core is **773/773**. Fresh zero-cache `pnpm check` passes. This is not registry provenance or transitive expansion; authoritative graph records, nested closures, Skill Pack/SubAgent segments, policy and final closure remain open.
    - Direct Agent→Skill Pack member increment: raw Agent/Pack sources are independently prepared, exact full-pin and selected projection/exposure policy are verified in one bounded Binding-set pass, then every member (including disabled/unexposed) receives `root → binding → skill_pack_member` through the pre-registered complete root namespace. Multiple Pack Bindings remain isolated and the exact 128-member source boundary passes. Focused path/Pack tests are **87/87** and release-core is **783/783**. Sealed operation routes, nested Pack member expansion, SubAgent targets, policy and final closure remain open.
    - Direct SubAgent target increment: internal Agent dependencies require exact prepared pins, reject direct same-version cycles and expand `root → parent binding → subagent_target → dependency binding`; external A2A dependencies pass full batched leaf Binding verification and end at `root → binding → subagent_target`. Both retain the complete parent namespace, source-disabled paths and deep freeze without inventing nested closure seals. Focused path/leaf tests are **134/134** and release-core is **801/801**. Transitive graph seals and effective delegation policy remain open.
    - Skill Pack operation-route increment: each exact selected exposure is joined to its Pack Binding path, exact member path, member target and both operation contract hashes. `route_hash` is canonical SHA-256 over the versioned six-field route preimage; routes are deterministic, root-namespace isolated and deeply frozen. Disabled Pack routes remain source facts for later effective-policy compilation. Release-core is **809/809**, fresh zero-cache `pnpm check` and architecture tests **31/31** pass. This private seal-ready intermediate is not publisher/registry authority; transitive closure, effective policy and final sealing remain open.
    - Pinned-graph direct-edge increment: a private graph-bound slice fully recomputes stored graph bytes, requires the requested root to equal the graph root, resolves exact full-pin nodes and proves a direct root→dependency edge. Skill Pack routes now compose this proof before becoming graph-bound, while nested Flow/Agent nodes preserve their supplied nested closure hash. Release-core is **817/817** and fresh zero-cache `pnpm check` passes. Registry snapshot provenance and publisher sealing remain external prerequisites.
    - All-direct-slice graph composition increment: Agent→Flow, internal Agent SubAgent and external A2A path adapters now compose the same graph-bound direct-edge proof. Flow/internal nodes retain the graph record's required nested closure hash; external A2A remains terminal and cannot acquire a fictitious nested seal. Focused graph-bound tests are **19/19** and release-core is **820/820**. Recursive verification of the referenced nested closure bytes remains open.
    - Agent GateSpec projection increment: all immutable Agent root gates compile into the closed `CompiledGateSpecEntryV1` shape with exact root resource node, ID/hash, kind, decision/approver/notification hashes, dispositions and canonical protected operation set. Root gates cannot carry Flow-only path/node fields; output remains private and unhashed until Binding coverage is joined. Release-core is **825/825**. Flow-node GateSpecs and approval Binding coverage remain open.
    - Flow GateSpec projection increment: recursively discovered human-gate nodes compile under the exact Flow resource node plus the full graph-aware `flow_node` path and node ID. Source validation precedes explicit structural projection, duplicate/invalid gate definitions fail closed, root-vs-Flow compiled variants remain disjoint and output remains private until operation coverage is joined. Focused Agent/Flow gate tests are **10/10** and release-core is **830/830**.
    - Agent Binding approval coverage increment: one exact Binding is re-derived from the Agent source, its operation pins are closed/unique, and approval is joined to exactly one same-source approval GateSpec by ID/hash with complete protected-operation coverage. Non-approval Bindings reject operations that intrinsically require approval; required approval rejects empty, duplicate or uncovered operation sets. Release-core is **836/836**. Per-kind operation collection into final Binding entries remains open.
    - Agent leaf-operation collection increment: Knowledge, Database, Plugin and external A2A sources are independently prepared, exact full-pin matching Bindings are batch-verified, and the leaf's concrete operation pin/intrinsic requirements are attached only to those matching canonical root paths. All sibling paths remain in the namespace with empty foreign operation sets; disabled paths remain source facts. Release-core is **843/843**. Flow/internal Agent/Pack operation aggregation remains open.
    - Skill Pack Binding-operation increment: prepared exact routes now also expose a canonical Binding-level operation set for final Binding entry assembly. Each selected exposure contributes its verified member operation pin; aliases that route to the same operation retain distinct routes but deduplicate the operation pin by contract hash. Multiple Pack mounts remain path-isolated and all outputs are deeply frozen. Release-core is **845/845**. Nested Pack closure/effective policy remains open.
    - Compiled/nested closure verification increment: supplied closure artifacts are copied under a dedicated 32 MiB/8,192-entry budget, losslessly parsed against the closed contract, checked by recomputing resource-node and available Binding-path identities, and verified by recomputing the complete closure hash. Agent/Flow nested joins bind version identity plus the graph-committed nested hash while intentionally allowing the registry published hash to differ from the closure-local semantic seed. Focused tests are **14/14** and release-core is **859/859**.
    - Closure canonical-set increment: a self-consistent hash no longer admits alternative permutations or duplicates. Assembly pins, Bindings, gates, resource nodes, dependency edges and disabled paths are checked against their explicit canonical keys before hash acceptance. Focused closure tests are **15/15** and release-core is **860/860**; fresh zero-cache `pnpm check` passes.
- [ ] **T4 — Compile gate, route and child policy boundaries.** Verify GateSpec source/ID/hash/operation coverage, exact Skill Pack operation routing and the frozen join-only child terminal map. Unknown or incomplete combinations fail closed. Files: compiler modules and focused adversarial fixtures. Depends on T3. Risk: L4.
- [ ] **T5 — Implement typed admission and ResolvedPlan narrowing.** Consume verified closure and Agent/Flow-specific deployment profiles; integrate existing credential/admission primitives; forbid new paths, targets or operations and retain epoch decision evidence. Files: release-core admission/plan modules and domain contracts/tests as required. Depends on T4. Risk: L4.
- [ ] **T6 — Integrate publisher evidence and review.** Add real registry/publisher readback and rejection fixtures; synchronize the architecture manifest with actual test inventory; run full gates and independent security, architecture, quality, performance and test reviews. Update only evidence-supported status. Files: relevant publisher/DB integration fixtures, `tests/architecture-gate/manifest.json`, handoff evidence. Depends on T5. Risk: L4.

## 5. Verification

Current T3.2 increment (still partial): bounded single-resource JSON Schema 2020-12 preparation/verification and real instance validation have explicit engine/version/profile hashes, isolated worker deadlines/admission and strict non-mutating validation. Operation/Strategy plus Agent/Flow/leaf/Skill Pack wrappers now bind every explicit Schema-bearing source field with complete, canonical evidence. Agent root Binding, recursive Flow node, Skill Pack member and internal/external SubAgent target paths are derived privately from raw validated source; direct dependency expansion uses exact full pins, the complete root Binding namespace and one closure-local identity registry. Flow identities include their graph namespace and preserve full structural ancestry. Exact Skill Pack exposure routes bind Pack/member paths, target and operation identities under a versioned per-route hash and supply deduplicated Binding-level operation pins. Pack, Flow and internal/external SubAgent direct slices can all require an exact direct edge in a fully recomputed pinned graph. Agent root and recursive Flow-node GateSpecs now have closed source-node/path projections, and Agent Binding approval joins require exact same-source gate operation coverage. Four Agent leaf kinds attach concrete operation pins and intrinsic requirements only to exact matching root paths. Batch boundaries include unique 8,194-schema, real 1,025-field Flow and exact 128-member Pack regressions. Observed release-core total is **845/845**, domain baseline is **155/155**. Remaining work includes Flow/internal Agent operation aggregation, final Binding entries/effective policies, recursive nested-seal verification, publisher sealing, registry/admission integration and application delivery. Schema validation results and source signatures are not host-attested reviewer evidence. No new PostgreSQL, upload, host-attested or production acceptance is claimed.

- Baseline observed this session: release-core has 33 passing tests. This is baseline evidence only, not G1 implementation coverage.
- The current working-tree `pnpm check` also completed successfully on 2026-09-02 (format, lint, workspace, contracts, typecheck, tests and build; Turbo cache reuse was enabled, with existing non-fatal lint warnings). It does not run the PostgreSQL suites or establish a fresh host attestation.
- Unit/contract tests: independent canonical vectors; unknown fields/kinds; Unicode and size boundaries; nested repeated IDs; permutation stability for sets and order sensitivity for paths; cycles; duplicate/colliding identities; missing or mixed deployment profiles; mutable latest rejection.
- Policy properties: idempotence, commutativity and associativity where the operation is a meet; no authority expansion in any dimension; read clearance and output taint tested in opposite directions. Required scopes and intrinsic resource requirements are validated separately from allow-set intersection.
- Integration/adversarial tests: hidden nested write capability, absent credential mapping, out-of-scope operation/egress, disabled optional vs mandatory paths, wrong GateSpec/route, immutable registry mismatch, cross-workspace references and runtime-added capability rejection.
- Admission matrix: Agent, top-level Flow and Agent-embedded Flow. Embedded Flow inherits its parent Plan and cannot borrow a top-level Flow profile or Workspace default credential. Mutate each kind/principal/audience/channel/scope/cardinality discriminator independently.
- Publishing atomicity: inject failures at closure blob/hash, root compiled hash, dependency manifest and registry writes. Independent readback must show no partial publication or sealed root; a successful retry must preserve matching pins/hashes across all four facts.
- Final evidence: all affected package tests, contract check, typecheck, lint, build, architecture mutation tests, six real PostgreSQL suites and the clean-checkout architecture gate. Review results must include the real user path and doc/code consistency; tests may not be skipped to accommodate local infrastructure failure.
- G1-A1 completion does not imply a public app exists. Full application delivery still requires authenticated browser/API/runtime/persistence E2E, deployment isolation, reload/restart persistence, cancellation/replay/recovery and production promotion evidence.

## 6. Risks and operational boundary

| Risk | Impact | Mitigation |
|---|---|---|
| Wrong authority database substituted for the original | Invalid admission conclusion | Preserve hashes and provenance; verify the original record; no self-created replacement. |
| Unspecified identity encoding details | Incompatible hashes or aliasing | Freeze byte grammar and independent vectors before implementation. |
| Policy dimensions treated uniformly | Clearance/taint inversion or credential widening | Explicit dimension-specific rules and property tests. |
| Unbounded nested closure inputs | Memory/CPU exhaustion | Absolute budgets and fail-closed validation before expansion. |
| UI or CI success mistaken for app completion | Incomplete user delivery | Separate per-layer evidence and the final real-browser vertical acceptance. |
| Full server root filesystem | Failed upload/migration or collateral damage | Keep deployment paused at disk preflight; partition expansion or cleanup requires specific authorization. |

Current server deployment remains untouched. This plan does not authorize partition changes, deleting neighboring projects, copying their credentials, opening database ports publicly or changing historical acceptance evidence.
