{
  "name": "@better-agent/domain-contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Versioned domain schemas for Better Agent release and runtime contracts",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build tsconfig.build.json --pretty false --stopBuildOnErrors",
    "lint": "biome lint src test vitest.config.ts",
    "test": "vitest run --config vitest.config.ts --configLoader native",
    "typecheck": "tsc --project tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "zod": "4.4.3"
  }
}
