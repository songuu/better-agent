openapi: 3.1.0
info:
  title: External reference fixture
  version: 1.0.0
paths:
  /widgets:
    get:
      operationId: listExternalWidgets
      responses:
        '200':
          description: External widgets
          content:
            application/json:
              schema:
                $ref: './external-components.yaml#/WidgetList'
