openapi: 3.1.0
info:
  title: Broken ref fixture
  version: 1.0.0
paths:
  /widgets:
    get:
      operationId: listWidgets
      responses:
        '200':
          description: Widgets
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MissingWidget'
components:
  schemas: {}
