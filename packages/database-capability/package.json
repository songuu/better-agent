{
  "name": "@better-agent/database-capability",
  "version": "0.0.0",
  "private": true,
  "description": "Sealed PostgreSQL read capability compiler for Better Agent",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build tsconfig.build.json --pretty false --stopBuildOnErrors",
    "lint": "biome lint src test vitest.config.ts",
    "test": "vitest run --config vitest.config.ts --configLoader native --passWithNoTests",
    "typecheck": "tsc --project tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@better-agent/domain-contracts": "workspace:*",
    "@better-agent/release-core": "workspace:*"
  }
}
