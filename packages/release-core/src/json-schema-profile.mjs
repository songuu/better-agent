export const JSON_SCHEMA_VALIDATOR_PROFILE = Object.freeze({
  schema_version: 'json-schema-validator-profile/1',
  dialect: 'https://json-schema.org/draft/2020-12/schema',
  ajv_version: '8.20.0',
  formats_version: '3.0.1',
  references: 'same-document-fragment-only',
  dynamic_references: 'single-resource-static-lowering',
  anchor_references: 'schema-location-pointer-lowering',
  nested_resource_ids: 'forbidden',
  schema_map_proto_keys: 'forbidden',
  data_mutation: 'forbidden',
  format_mode: 'full',
  formats: Object.freeze([
    'date',
    'time',
    'date-time',
    'duration',
    'uri',
    'uri-reference',
    'uri-template',
    'email',
    'hostname',
    'ipv4',
    'ipv6',
    'uuid',
    'regex',
    'json-pointer',
    'relative-json-pointer',
  ]),
  maximum_schema_nodes: 4096,
  maximum_operand_bytes: 8_388_608,
  worker_deadline_ms: 5000,
  maximum_workers: 4,
  resource_limits: Object.freeze({
    maxOldGenerationSizeMb: 128,
    maxYoungGenerationSizeMb: 16,
    stackSizeMb: 4,
  }),
  ajv_options: Object.freeze({
    strictSchema: true,
    strictNumbers: true,
    strictTypes: false,
    strictTuples: false,
    strictRequired: false,
    allowUnionTypes: true,
    allowMatchingProperties: true,
    allErrors: false,
    validateSchema: true,
    validateFormats: true,
    useDefaults: false,
    coerceTypes: false,
    removeAdditional: false,
    ownProperties: true,
    inlineRefs: false,
    unicodeRegExp: true,
    messages: false,
    logger: false,
  }),
});
